<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.commerce.mall.portal.dao.PortalOrderDao">
    <resultMap id="orderDetailMap" type="com.commerce.mall.portal.domain.OmsOrderDetail"
               extends="com.commerce.mall.mapper.OmsOrderMapper.BaseResultMap">
        <collection property="orderItemList" columnPrefix="ot_"
                    resultMap="com.commerce.mall.mapper.OmsOrderItemMapper.BaseResultMap">
        </collection>
        <!--<collection property="orderItemList"
                    ofType="OmsOrderItem">
        </collection>-->
    </resultMap>
    <select id="getDetail" resultMap="orderDetailMap">
        SELECT
            o.id,
            o.order_sn,
            o.coupon_id,
            o.integration,
            o.member_id,
            ot.id ot_id,
            ot.product_name ot_product_name,
            ot.product_sku_id ot_product_sku_id,
            ot.product_sku_code ot_product_sku_code,
            ot.product_quantity ot_product_quantity
        FROM
            oms_order o
            LEFT JOIN oms_order_item ot ON o.id = ot.order_id
        WHERE
            o.id = #{orderId}
    </select>

    <select id="getTimeOutOrders" resultMap="orderDetailMap">
        SELECT
            o.id,
            o.order_sn,
            o.coupon_id,
            o.integration,
            o.member_id,
            o.use_integration,
            ot.id               ot_id,
            ot.product_name     ot_product_name,
            ot.product_sku_id   ot_product_sku_id,
            ot.product_sku_code ot_product_sku_code,
            ot.product_quantity ot_product_quantity
        FROM
            oms_order o
            LEFT JOIN oms_order_item ot ON o.id = ot.order_id
        WHERE
            o.status = 0
            AND o.create_time &lt; date_add(NOW(), INTERVAL -#{minute} MINUTE);
    </select>

    <update id="updateSkuStock">
        UPDATE pms_sku_stock
        SET
            stock = CASE id
            <foreach collection="itemList" item="item">
              WHEN #{item.productSkuId} THEN stock - #{item.productQuantity}
            </foreach>
            END,
            lock_stock = CASE id
            <foreach collection="itemList" item="item">
              WHEN #{item.productSkuId} THEN lock_stock - #{item.productQuantity}
            </foreach>
            END
        WHERE
            id IN
        <foreach collection="itemList" item="item" separator="," open="(" close=")">
            #{item.productSkuId}
        </foreach>
    </update>
    <update id="updateOrderStatus">
        update oms_order
        set status=#{status}
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>
    <update id="releaseSkuStockLock">
        UPDATE pms_sku_stock
        SET
        lock_stock = CASE id
        <foreach collection="itemList" item="item">
            WHEN #{item.productSkuId} THEN lock_stock - #{item.productQuantity}
        </foreach>
        END
        WHERE
        id IN
        <foreach collection="itemList" item="item" separator="," open="(" close=")">
            #{item.productSkuId}
        </foreach>
    </update>
    <update id="deleteOrder">
        UPDATE oms_order
        SET
        delete_status=1
        WHERE
        id=#{OrderId}
    </update>
    <update id="Received">
        UPDATE oms_order
        SET
        status=3
        WHERE
        id=#{OrderId}
    </update>
    <update id="cancelOneOrder">
        UPDATE oms_order
        SET
        status=4
        WHERE
        id=#{OrderId}
    </update>

    <!--订单状态：0->待付款；1->待发货；2->已发货；3->已完成；4->已关闭；5->无效订单
-->
    <select id="getNoPayList" resultMap="orderDetailMap">
        SELECT
        o.create_time ,
        o.id oid,
        o.`status`,
        ooi.product_pic ot_product_pic,
        ooi.detail_title ot_detail_title,
        ooi.product_attr ot_product_attr,
        ooi.product_price ot_product_price,
        ooi.product_quantity ot_product_quantity
        FROM
        oms_order  o
        INNER JOIN oms_order_item ooi ON o.id=ooi.order_id
        AND
        o.`status` = 0
        AND
        o.member_id = #{userId}
        WHERE
        o.delete_status = 0
        ORDER BY
        o.create_time
        DESC
    </select>
    <select id="getAllList" resultMap="orderDetailMap">
        SELECT
        o.create_time ,
        o.id,
        o.`status`,
        ooi.product_pic ot_product_pic,
        ooi.detail_title ot_detail_title,
        ooi.product_attr ot_product_attr,
        ooi.product_price ot_product_price,
        ooi.product_quantity ot_product_quantity
        FROM
        oms_order  o
        INNER JOIN oms_order_item ooi ON o.id=ooi.order_id
        AND
        o.member_id = #{userId}
        WHERE
        o.delete_status = 0
        ORDER BY
        o.create_time
        DESC
    </select>
    <select id="getNoReceivedList" resultMap="orderDetailMap">
        SELECT
        o.create_time ,
        o.id oid,
        o.`status`,
        ooi.product_pic ot_product_pic,
        ooi.detail_title ot_detail_title,
        ooi.product_attr ot_product_attr,
        ooi.product_price ot_product_price,
        ooi.product_quantity ot_product_quantity
        FROM
        oms_order  o
        INNER JOIN oms_order_item ooi ON o.id=ooi.order_id
        AND
        o.`status` = 2
        AND
        o.member_id = #{userId}
        WHERE
        o.delete_status = 0
        ORDER BY
        o.create_time
        DESC
    </select>
    <select id="getNoEvaluateList" resultMap="orderDetailMap">
        SELECT
        o.create_time ,
        o.id oid,
        o.`status`,
        ooi.product_pic ot_product_pic,
        ooi.detail_title ot_detail_title,
        ooi.product_attr ot_product_attr,
        ooi.product_price ot_product_price,
        ooi.product_quantity ot_product_quantity
        FROM
        oms_order  o
        INNER JOIN oms_order_item ooi ON o.id=ooi.order_id
        AND
        o.`status` = 3
        AND
        o.member_id = #{userId}
        WHERE
        o.delete_status = 0
        ORDER BY
        o.create_time
        DESC
    </select>
</mapper>