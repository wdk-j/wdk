<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.commerce.mall.dao.TmsFoodCommentDetailDao">
    <resultMap id="BaseResultMap" type="com.commerce.mall.dto.TmsFoodCommentDetail">
        <id column="comm_id" jdbcType="INTEGER" property="commId"/>
        <result column="post_time" jdbcType="TIMESTAMP" property="postTime"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="food_id" jdbcType="INTEGER" property="foodId"/>
        <result column="reply" jdbcType="VARCHAR" property="reply"/>
        <result column="nickname" jdbcType="VARCHAR" property="nickname"/>
        <collection property="commPics" column="{commId=comm_id}"
                    select="com.commerce.mall.mapper.TmsFoodCommentsPicsMapper.selectByCommId"/>
    </resultMap>
    <sql id="Base_Column_List">
        comm_id, content, post_time, user_id, food_id, reply
    </sql>
    <update id="updateReplyByPrimaryKey">
        update tms_food_comments
        set reply=#{reply,jdbcType=VARCHAR}
        where comm_id = #{commId}
    </update>
    <select id="selectCommentsDetailed" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>,f.seller_id,m.nickname
        from tms_food_comments fc
        left join tms_food f on fc.food_id=f.food_id
        left join ums_member m on fc.user_id=m.id
        <where>
            <if test="keyword!=null">
                <bind name="keyword" value="'%'+keyword+'%'"/>
                and f.food_name like #{keyword}
            </if>
            <if test="foodId!=null">
                and fc.food_id=#{foodId}
            </if>
            and f.seller_id=#{sellerId}
        </where>
        order by post_time desc
    </select>
    <select id="selectByPrimaryKey" resultType="com.commerce.mall.dto.TmsFoodCommentDetail">
        select
        <include refid="Base_Column_List"/>, m.nickname
        from tms_food_comments f left join ums_member m on f.user_id=m.id
        where comm_id=#{commId}
    </select>
</mapper>